namespace SwiftPantry.PlaywrightTests.Helpers;

/// <summary>Shared helper utilities for Playwright tests.</summary>
public static class TestHelpers
{
    // TODO: Add shared helper methods as needed by test suites.
    // Examples:
    //   - ExtractAntiForgeryToken(IPage page)
    //   - WaitForFlashMessage(IPage page, string expectedText)
    //   - AcceptNextDialog(IPage page)
}
